# KDE-Epson-FAX

Repository bootstrap for the KDE-Epson-FAX project.

## Current status

At the moment, the repository only contains a `.gitkeep` placeholder and no source code yet.

## Suggested next structure

- `src/` for application code
- `tests/` for automated tests
- `docs/` for architecture and onboarding notes
- `scripts/` for developer tooling

## Onboarding checklist

1. Define project goals and target runtime/environment.
2. Add an initial module skeleton with a minimal runnable entrypoint.
3. Add CI checks (formatting, linting, tests).
4. Document local development workflow.
